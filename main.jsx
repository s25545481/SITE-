import React from "react";
import ReactDOM from "react-dom/client";
import OldTeamApp from "./OldTeamApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OldTeamApp />
  </React.StrictMode>
);